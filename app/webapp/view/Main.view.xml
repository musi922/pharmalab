<mvc:View
  controllerName="com.moyo.demo.myfiorielementsproject.controller.Main"
  xmlns:mvc="sap.ui.core.mvc"
  xmlns="sap.m"
  xmlns:f="sap.f"
  xmlns:layout="sap.ui.layout"
  displayBlock="true"
>
  <App id="app">
    <pages>
      <f:DynamicPage
        id="dynamicPageId"
        headerExpanded="true"
        toggleHeaderOnTitleClick="true"
      >
        <f:title>
          <f:DynamicPageTitle>
            <f:heading>
              <Title text="{i18n>mainViewTitle}" level="H2" />
            </f:heading>
          </f:DynamicPageTitle>
        </f:title>

        <f:header>
          <f:DynamicPageHeader pinnable="true">
            <layout:HorizontalLayout>
              <layout:VerticalLayout class="sapUiMediumMarginEnd">
                <Label text="Search" />
                <SearchField placeholder="Filter by Name..." search=".onSearch" width="250px" />
              </layout:VerticalLayout>
            </layout:HorizontalLayout>
          </f:DynamicPageHeader>
        </f:header>

        <f:content>
          <Table
            id="mainTable"
            inset="false"
            items="{/Data}"
            class="sapFDynamicPageAlignContent"
            width="auto"
          >
            <headerToolbar>
              <OverflowToolbar>
                <Title text="Items" level="H2" />
                <ToolbarSpacer />
                <Button icon="sap-icon://add" text="Add Item" press=".onAddPress" />
                <Button icon="sap-icon://sort" press=".onSortPress" />
              </OverflowToolbar>
            </headerToolbar>
            <columns>
              <Column>
                <Text text="ID" />
              </Column>
              <Column>
                <Text text="Name" />
              </Column>
              <Column>
                <Text text="Description" />
              </Column>
              <Column>
                <Text text="Status" />
              </Column>
            </columns>
            <items>
              <ColumnListItem>
                <cells>
                  <Text text="{ID}" />
                  <Text text="{Name}" />
                  <Text text="{Description}" />
                  <ObjectStatus text="{Status}" state="{= ${Status} === 'Active' ? 'Success' : 'Warning' }" />
                </cells>
              </ColumnListItem>
            </items>
          </Table>
        </f:content>
      </f:DynamicPage>
    </pages>
  </App>
</mvc:View>