{
  "name": "my-fiorielements-project",
  "version": "1.0.0",
  "description": "my-fiorielements-project",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^9.9.1",
    "@sap/cds-foss": "^5.0.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2.4.0",
    "@sap/cds-dk": "^9.9.0",
    "@ui5/cli": "^4.0.52",
    "eslint": "^9.39.4",
    "eslint-plugin-ui5": "^0.1.0"
  },
  "scripts": {
    "start": "cds watch",
    "build": "npm run build:ui5",
    "build:ui5": "cd app && npm install && npm run build"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "credentials": {
          "database": ":memory:"
        }
      }
    }
  }
}
